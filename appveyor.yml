init:
  - git config --global core.autocrlf true

branches:
  only:
    - appveyor
    - master

before_build:
  - '"%VS140COMNTOOLS%VsDevCmd.bat"'
  - BeforeBuild.cmd

platform: Any CPU

configuration:
#  - Debug
  - Release

build:
  project: src/Everything.sln
  
test:
  assemblies:
    - '**\Sarif.UnitTests.dll'
    - '**\Sarif.Driver.UnitTests.dll'
    - '**\Sarif.FunctionalTests.dll'
    - '**\Sarif.ValidationTests.dll'
    
artifacts:
  - path: 'bld\bin'
  
  - path: 'TestResults\*.trx'

notifications:
  - provider: Email
    to:
#      - vsarcplat@microsoft.com
       - rolsthoorn12@gmail.com
    subject: 'Sarif-SDK Build {{status}}'
    message: 'In reference to {{commitID}}. View log output <a href="{{buildURL}}">here</a> '
    on_build_status_changed: true

cache:
  - packages -> **\packages.config
  - '%LocalAppData%\NuGet\Cache'
