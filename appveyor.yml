branches:

  only:
    - appveyor

build:
  project: src/Sarif.Sdk.sln

build_script:
  - '"%VS140COMNTOOLS%VsDevCmd.bat"'
  - BuildAndTest.cmd
test: off

artifacts:
  - path: logs.txt
    name: test
